# bank-marketing-svm-model
Machine Learning project based on the Bank Marketing dataset. Built an SVM model to predict whether a customer will subscribe to a term deposit. Includes steps like data preprocessing, encoding, scaling, and hyperparameter tuning to achieve high accuracy.
